# Install a CUDA-enabled PyTorch build appropriate for the target GPU first.
torch>=2.5.1
torchvision>=0.20.1
numpy
opencv-python
pillow
openai

# Install the official SAM 2 repository separately and record the exact commit:
#   git clone https://github.com/facebookresearch/sam2.git
#   cd sam2 && pip install -e .
# Download a matching SAM 2.1 checkpoint (small is the recommended starting point).
